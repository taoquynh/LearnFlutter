main() {
//   Dart cung cấp lệnh Assert(biểu_thức_logic); để khi chạy mà biểu thức logic sai sẽ dừng chương trình ở đó. 
//   Assert là cách để kiểm tra một biểu thức, 
//   vấn đề là nó không có ảnh hưởng gì khi chạy ở chế độ product nó chỉ tác dụng khi phát triển (Chạy debug Ctrl + F5 trong VS)

// Đảm bảo một đối tượng khác null
// assert(myobject != null);

// Đảm bảo số lớn hơn 100
var number = 101;

}